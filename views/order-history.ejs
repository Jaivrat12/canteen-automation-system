<!DOCTYPE html>
<html lang="en">

<%- include("partials/head.ejs") %>

    <body>

        <%- include('partials/appbar.ejs') %>

        <h1 class="heading">ORDER HISTORY</h1>
        <% for( let i=0; i < orders.length; i++ ) { %>
            <div class="order">
                <div class="order-row">
                    <div class="order-id">
                        order id: <%= orders[i].id %>
                    </div>
                    <div class="status">
                        status: <%= orders[i].status %>
                    </div>
                </div>

                <% for( let i=0; i < orders[i].items.length; i++ ) { %>

                    <div class="item">
                        <div>
                            <%= orders[i].items[i].quantity %> x <%= orders[i].items[i].name %>
                        </div>
                        <div>
                            Rs <%= orders[i].items[i].price %>
                        </div>
                    </div>

                <% } %>

                <hr>

                <div class="total-price">
                    total Rs <%= orders[i].total %>
                </div>

                placed at : <%= orders[i].createdAt %>
            </div>

        <% } %>

    </body>

</html>